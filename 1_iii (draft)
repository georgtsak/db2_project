--Βρείτε ποια πλοία ανέπτυξαν κάποια στιγμή ταχύτητα άνω των 30 κόμβων,
--τι τύπου ήταν το κάθε πλοίο και
--πόσα ήταν αυτά τα πλοία ανά τύπο.
--
--
--εμφανίζει σωστά τις στήλες vessel_id και description αλλά εμφανίζει λάθος num_each_type:
--
select v.id, description,count(v.type)
from Positions as p
inner join Vessels as v on p.vessel_id = v.id
inner join VesselTypes as vt on v.type = vt.code
where speed > 30 
group by type,v.id,description; 
--
--
--εμφανίζει σωστά τις στήλες description και num_each_type αλλά λείπει το vessel_id: 
--
select vt.description as vessel_type, count(distinct p.vessel_id) as num_each_type
from Positions as p
inner join Vessels as v on p.vessel_id = v.id
inner join VesselTypes as  vt on v.type = vt.code
where p.speed > 30
group by vt.description
having count(distinct p.vessel_id) > 0;
